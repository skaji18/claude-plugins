---
description: Set up permission-guard — create venv, install deps, generate global/project config templates, and run tests
---

Run the setup script and display its output:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh"
```

Report the test results (pass/fail count). Setup is complete when all tests pass.
